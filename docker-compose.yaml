services:
  backend:
    image: ${DOCKER_IMAGE}        # ← полная ссылка: username/backend:sha-abc123
    restart: unless-stopped
    ports:
      - "4545:4545"
    env_file: .env
    networks:
      - app-network
networks:
  app-network: